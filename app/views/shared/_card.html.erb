
<div class="card-event-old card-home-old <%= 'past-event' if event.end < DateTime.now %>">
  <div class='event-image' style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path event.photo.key%>')">
   <div class="card-info">
     <div class="event-text mx-3">
      <h2> <%= "#{I18n.localize(event.start, format: "%^a %d %^b %Y")} à #{event.start.strftime("%I:%M %p")} "%></h2>
      <h1 class="card-title mb-0"><%= event.name %></h1>
     </div>
      <p class ="m-2"> <img src="https://img.icons8.com/cotton/64/000000/person-male--v2.png" width="25px"><%= "#{event.reservations.count}/#{event.max_participants}" %></p>
    </div>
   </div>
  <%= link_to "", event_path(event), class: "card-link" %>
</div>

