 <div class="d-flex justify-content-between">
        <%= link_to event_reservation_path(@event), method: :delete, remote: false do%>
          <div class="btn-desinscrire">
            <i class="fas fa-check"></i><%= " Participe" %>
          </div>
        <% end %>
        <%= render 'shared/chat_btn'  %>
      </div>
