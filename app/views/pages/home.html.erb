<div class="body-home">

  <div class="text-home">

    <div class="slogan">
      <div class="first-home">
        <div class="slogan-text">
            <div id="typed-strings">
              <h1><strong>Happy</strong></h1>
              <h1><strong>Humain</strong></h1>
              <h1><strong>Hôpital</strong></h1>
              <h1><strong>Hhhhhh</strong></h1>
              <h1><strong>Humain</strong></h1>
              <h1><strong>Hôpital</strong></h1>
            </div>
            <span id="typed"></span>
            <span><strong>...place</strong></span>
        </div>
      </div>
    </div>
    <p style="padding: 6em;"><em>En établissement hospitalier aussi, on peut être bien entouré</em></p>
  </div>

  <div class="search-bar-homepage">
    <%= form_tag handler_path, method: :get do %>
      <div class="d-flex">
        <%= text_field_tag :query, params[:query], class: "form-control", placeholder: "Votre hôpital"%>
        <button name="button" type="submit" class="icone-loupe">
          <i class="fas fa-search"></i>
        </button>
      </div>
    <% end %>
  </div>
</div>


<div class="top-events-new">
  <h2 style="margin-top: 1em; margin-left: 1em; margin-bottom: 1em;">Événements à venir</h2>
  <div class="cards-new container">
    <% best_events = [Event.find_by(name: "Belotte"), Event.find_by(name: "Tricot"), Event.find_by(name: "Uno")] %>
    <% best_events.each do |event| %>
      <%= render  'shared/card_new_event', event: event%>
    <% end %>
  </div>
</div>
